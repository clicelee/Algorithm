const solution = (s) => Number(s);

/* genius solution */
function strToInt(str){
  return str/1
  }
JavaScript에서는 산술 연산자가 피연산자 중 하나가 숫자가 아닌 경우, 이를 숫자로 변환하려고 시도한다. 
따라서 어떤 값을 / 연산자를 사용하여  연산하려고 하면 강제로 숫자로 변환해준다
