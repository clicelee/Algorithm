const solution=(strlist)=>strlist.map(function(value){ return value.length});
//return array.map(function(i){ return array[i].length});
//map(function(i))에서 i는 그 자체로 배열의 원소이다


// const solution=(strlist)=> strlist.map((el) => el.length);
// 배열의 원소element를 el로한다
